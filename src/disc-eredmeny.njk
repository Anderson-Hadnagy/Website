---
layout: layouts/base.njk
title: A Te Személyiségprofilod | DISC
permalink: /disc-eredmeny/
---

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<style>
  main {
    max-width: 100% !important;
    padding-left: 1rem !important;
    padding-right: 1rem !important;
  }

  .result-section {
    max-width: 1400px;
    margin: 0 auto; 
    padding: 2rem 0;
  }

  .sector-title {
    color: #fff;
    font-size: 1.8rem;
    margin: 4rem 0 2rem 0;
    padding-left: 1rem;
    border-left: 4px solid var(--primary);
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .result-card {
    background: var(--bg-card);
    border-radius: 24px;
    padding: 3.5rem;
    box-shadow: 0 15px 35px -10px rgba(0, 0, 0, 0.5);
    border: 1px solid rgba(255, 255, 255, 0.05);
    margin-bottom: 2rem;
  }

  .grid-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start; /* Changed to start for better alignment with longer lists */
  }

  .sector-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .info-block {
    background: rgba(255, 255, 255, 0.02);
    padding: 2rem;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.03);
    transition: transform 0.3s ease, background 0.3s ease;
  }

  .info-block:hover {
    background: rgba(255, 255, 255, 0.04);
    transform: translateY(-5px);
  }

  .info-block h4 {
    font-size: 1.25rem;
    margin-bottom: 1.2rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .info-block p, .info-block li {
    color: var(--text-muted);
    font-size: 1.05rem;
    line-height: 1.7;
  }

  /* Progress Bar Styles */
  .bar-container {
    width: 100%; 
    background: rgba(255,255,255,0.05); 
    border-radius: 50px; 
    height: 14px; 
    overflow: hidden;
    margin-top: 0.5rem;
  }
  .bar-fill {
    height: 100%; 
    border-radius: 50px; 
    transition: width 1.5s ease-out; 
    width: 0%;
  }

  /* Tag style for Fears/Keywords */
  .tag-box {
    display: inline-block;
    padding: 4px 8px;
    border-radius: 6px;
    background: rgba(255,255,255,0.05);
    font-size: 0.85rem;
    margin-top: 0.5rem;
  }

  @media (max-width: 1100px) { .sector-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 768px) { .grid-stats, .sector-grid { grid-template-columns: 1fr; } }
</style>

<section class="result-section">
  <div id="resultsContainer" style="display: none;">
    
    <div id="mainCard" class="result-card" style="text-align: center;">
      <h1 id="mainResultTitle" style="font-size: 4rem; margin-bottom: 1.5rem;"></h1>
      <p id="mainResultDesc" style="font-size: 1.3rem; line-height: 1.8; color: var(--text-main); max-width: 1100px; margin: 0 auto;"></p>
    </div>

    <h2 class="sector-title">Vizuális Analitika</h2>
    <div class="result-card grid-stats">
      
      <div>
        <h3 style="color: #fff; margin-bottom: 2rem;">Százalékos profilod</h3>
        <div id="percentageBars"></div>
      </div>

      <div>
        <h3 style="color: #fff; margin-bottom: 2rem; text-align: center;">Dinamikus Pozicionálás</h3>
        <div style="background: rgba(255,255,255,0.02); padding: 20px; border-radius: 20px;">
          <canvas id="discChart" width="100" height="100"></canvas>
        </div>
      </div>
    </div>

    <h2 class="sector-title">Személyiség és Társadalom</h2>
    <div class="sector-grid">
      <div class="info-block">
        <h4 id="h4_1">🎭 Te vs. a Világ</h4>
        <p id="perceptionText"></p>
      </div>
      <div class="info-block">
        <h4 id="h4_2">🔥 Belső Hajtóerő</h4>
        <p id="motivationText"></p>
      </div>
      <div class="info-block">
        <h4 id="h4_3">⚠️ Kommunikáció</h4>
        <p id="commText"></p>
      </div>
    </div>

    <h2 class="sector-title">A Stressz Három Arca</h2>
    <div class="sector-grid">
      <div class="info-block">
        <h4 id="h4_4">⚡ Azonnali Reakció</h4>
        <p id="stressImmediateText"></p>
      </div>
      <div class="info-block">
        <h4 id="h4_5">🏋️ Nyomás Alatt</h4>
        <p id="stressPressureText"></p>
      </div>
      <div class="info-block">
        <h4 id="h4_6">🪫 Krónikus Stressz</h4>
        <p id="stressLongTermText"></p>
      </div>
    </div>

    <h2 class="sector-title">Életút és Kapcsolatok</h2>
    <div class="sector-grid">
      <div class="info-block">
        <h4 id="h4_7">⚔️ Konfliktusok</h4>
        <p id="conflictText"></p>
      </div>
      <div class="info-block">
        <h4 id="h4_8">❤️ Szerelem</h4>
        <p id="relationshipText"></p>
      </div>
      <div class="info-block">
        <h4 id="h4_9">💼 Karrier</h4>
        <div id="jobsText"></div>
      </div>
    </div>

    <div style="text-align: center; margin-top: 5rem;">
      <a href="/disc/" class="btn" style="background: transparent; border: 1px solid var(--text-muted); color: var(--text-main); padding: 15px 30px; border-radius: 50px; text-decoration: none; transition: 0.3s;">&larr; Teszt újrakitöltése</a>
    </div>
  </div>
</section>

<script>
document.addEventListener("DOMContentLoaded", () => {
  const params = new URLSearchParams(window.location.search);
  
  // Basic validation
  if(!params.has('D')) { window.location.href = '/disc/'; return; }

  // Parse Scores
  const scores = { 
    D: parseInt(params.get('D')) || 0, 
    I: parseInt(params.get('I')) || 0, 
    S: parseInt(params.get('S')) || 0, 
    C: parseInt(params.get('C')) || 0 
  };
  
  const total = scores.D + scores.I + scores.S + scores.C;

  // Expanded Data Object
  const colorsData = {
    D: { 
      name: 'PIROS (Domináns)', color: '#ef4444', 
      keywords: "Eredmény, Irányítás, Kihívás",
      motto: "Csináljuk meg most!",
      fear: "Irányítás elvesztése, Kudarc",
      desc: "Te egy született hajtóerő vagy, aki számára a világ egy megoldandó feladvány vagy egy legyőzendő akadálypálya. <strong>Rendkívül eredményorientált</strong>, határozott és magabiztos személyiség vagy. Nem ijedsz meg a felelősségtől, sőt, krízishelyzetben te vagy az, aki ösztönösen átveszi az irányítást.",
      perception: "<strong>Ahogy te látod magad:</strong> Céltudatos, logikus, gyors és őszinte vezető.<br><br><strong>Ahogy mások láthatnak:</strong> Távolságtartónak, arrogánsnak vagy türelmetlennek.",
      motivation: "A <strong>győzelem, a kontroll és a konkrét eredmények</strong> éltetnek.",
      comm: "Légy türelmesebb! Hallgass meg másokat végig, mielőtt döntesz.",
      stressImmediate: "Dühkitörés, követelőzés, mások szavának elfojtása.",
      stressPressure: "Csőlátás: csak a feladat számít, az emberek nem.",
      stressLongTerm: "Kiégés és elszigetelődést, mert senkit nem tartasz elég jónak.",
      conflict: "A konfliktus egy verseny, amit meg kell nyerni.",
      relationship: "Erős, önálló társra van szükséged, aki nem ijed meg tőled.",
      jobs: "<ul><li>Vállalkozó / Cégvezető</li><li>Projektmenedzser</li><li>Ügyvéd</li><li>Értékesítési vezető</li></ul>"
    },
    I: { 
      name: 'SÁRGA (Befolyásoló)', color: '#eab308', 
      keywords: "Lelkesedés, Társaság, Optimizmus",
      motto: "Csináljuk együtt és legyen jó buli!",
      fear: "Elutasítás, Unalom, Népszerűség vesztés",
      desc: "Te vagy a társaság motorja, az örök optimista. <strong>Imádod az embereket</strong>, a pörgést és a folyamatos impulzusokat. Zseniális meggyőzőképességgel rendelkezel, könnyen teremtesz kapcsolatokat, és te vagy az, aki képes lelkesíteni egy egész csapatot.",
      perception: "<strong>Ahogy te látod magad:</strong> Barátságos, inspiráló, kreatív és nyitott.<br><br><strong>Ahogy mások láthatnak:</strong> Felszínesnek, kapkodónak vagy túl hangosnak.",
      motivation: "Az <strong>elismerés, a népszerűség és az élmények</strong> hajtanak.",
      comm: "Gyakorold az aktív hallgatást és a részletekre való odafigyelést!",
      stressImmediate: "Érzelmi kitörés, szóáradat, viccelődés komoly helyzetben.",
      stressPressure: "Szétszórtság, kapkodás, ígéretek be nem tartása.",
      stressLongTerm: "Pesszimizmus, kedvtelenség, visszahúzódás.",
      conflict: "Megpróbálod elsimítani a bajt, vagy érzelmileg megsértődsz.",
      relationship: "Izgalomra és állandó megerősítésre vágysz.",
      jobs: "<ul><li>Marketing / PR</li><li>Rendezvényszervező</li><li>Tréner / Előadó</li><li>Utazásszervező</li></ul>"
    },
    S: { 
      name: 'ZÖLD (Stabil)', color: '#22c55e', 
      keywords: "Nyugalom, Támogatás, Lojalitás",
      motto: "Csináljuk békésen és biztosan.",
      fear: "Hirtelen változás, Biztonság elvesztése",
      desc: "Nyugodt, türelmes és végtelenül megbízható személyiség vagy. Számodra a <strong>harmónia és az őszinteség</strong> alapvető létszükségletek. Te vagy a tökéletes csapatjátékos, lojalitásod megingathatatlan.",
      perception: "<strong>Ahogy te látod magad:</strong> Megbízható, türelmes, lojális segítő.<br><br><strong>Ahogy mások láthatnak:</strong> Túl passzívnak, lassúnak vagy makacsnak.",
      motivation: "A <strong>biztonság, a stabilitás és a kiszámíthatóság</strong> éltet.",
      comm: "Tanulj meg nemet mondani és kiállni a saját érdekeidért!",
      stressImmediate: "Lefagyás, passzivitás, behódolás a békességért.",
      stressPressure: "Passzív-agresszivitás, makacs ellenállás.",
      stressLongTerm: "Mártírszerep és pszichoszomatikus tünetek.",
      conflict: "Kerülöd a konfrontációt, inkább engedsz.",
      relationship: "Hűséget, nyugalmat és mély elköteleződést keresel.",
      jobs: "<ul><li>HR szakértő</li><li>Tanár / Terapeuta</li><li>Ügyfélszolgálat</li><li>Egészségügyi dolgozó</li></ul>"
    },
    C: { 
      name: 'KÉK (Szabálytisztelő)', color: '#3b82f6', 
      keywords: "Precizitás, Elemzés, Logika",
      motto: "Csináljuk meg tökéletesen.",
      fear: "Kritika, Hibázás, Rendetlenség",
      desc: "Precíz, analitikus és mélyen logikus gondolkodású vagy. Számodra a <strong>pontosság és a tények</strong> mindennél többet érnek. Rád a legbonyolultabb feladatokat is rá lehet bízni, mert addig nem nyugszol, amíg minden részlet a helyére nem kerül.",
      perception: "<strong>Ahogy te látod magad:</strong> Objektív, felkészült, minőségi szakértő.<br><br><strong>Ahogy mások láthatnak:</strong> Hidegnek, szőrszálhasogatónak vagy kritikusnak.",
      motivation: "A <strong>pontosság, a rend és a logika</strong> motivál.",
      comm: "Fogadd el, hogy a 'jó' néha elég, nem kell mindig a 'tökéletes'.",
      stressImmediate: "Visszavonulás, bezárkózás, mániákus elemzés.",
      stressPressure: "Analízis-paralízis: képtelen vagy dönteni a félelemtől.",
      stressLongTerm: "Teljes izoláció és ellenséges kritika mindenki felé.",
      conflict: "Tényekkel vitázol, lefagysz az érzelmektől.",
      relationship: "Kiszámíthatóságot és intellektuális társat keresel.",
      jobs: "<ul><li>Mérnök / IT</li><li>Pénzügyi elemző</li><li>Minőségbiztosítás</li><li>Kutató / Tudós</li></ul>"
    }
  };

  // Sort scores to find Dominant and order the bars
  const sorted = Object.keys(scores)
    .map(key => ({ key, score: scores[key], percent: Math.round((scores[key] / total) * 100) }))
    .sort((a, b) => b.score - a.score);

  const dom = colorsData[sorted[0].key];

  // --- 1. Fill Main UI Elements ---
  document.getElementById('mainResultTitle').innerText = "Te a " + dom.name + " típus vagy!";
  document.getElementById('mainResultTitle').style.color = dom.color;
  document.getElementById('mainResultDesc').innerHTML = dom.desc;
  document.getElementById('mainCard').style.borderTop = `8px solid ${dom.color}`;

  // Fill Text Blocks
  document.getElementById('perceptionText').innerHTML = dom.perception;
  document.getElementById('motivationText').innerHTML = dom.motivation;
  document.getElementById('commText').innerHTML = dom.comm;
  document.getElementById('stressImmediateText').innerHTML = dom.stressImmediate;
  document.getElementById('stressPressureText').innerHTML = dom.stressPressure;
  document.getElementById('stressLongTermText').innerHTML = dom.stressLongTerm;
  document.getElementById('conflictText').innerHTML = dom.conflict;
  document.getElementById('relationshipText').innerHTML = dom.relationship;
  document.getElementById('jobsText').innerHTML = dom.jobs;

  // Colorize Sector Headings
  for(let i=1; i<=9; i++) { 
    const h4 = document.getElementById('h4_'+i);
    if(h4) {
      h4.style.color = dom.color;
      h4.style.borderBottom = `1px solid ${dom.color}33`;
    }
  }

  document.getElementById('resultsContainer').style.display = 'block';

  // --- 2. Generate Expanded Percentage Bars ---
  const barsContainer = document.getElementById('percentageBars');
  barsContainer.innerHTML = ''; // Reset

  sorted.forEach(item => {
    const c = colorsData[item.key];
    // Reduce opacity for traits with very low scores to visualize dominance better
    const opacity = item.percent > 20 ? 1 : 0.7;

    barsContainer.innerHTML += `
      <div style="margin-bottom: 2.5rem; opacity: ${opacity};">
        
        <div style="display: flex; justify-content: space-between; margin-bottom: 0.5rem; align-items: flex-end;">
          <strong style="color: #fff; font-size: 1.1rem; text-transform: uppercase; letter-spacing: 1px;">
            ${c.name}
          </strong>
          <span style="font-weight: 800; font-size: 1.2rem; color: ${c.color};">${item.percent}%</span>
        </div>
        
        <div class="bar-container">
          <div class="bar-fill" style="background: ${c.color}; box-shadow: 0 0 15px ${c.color}40;" data-width="${item.percent}%"></div>
        </div>

        <div style="margin-top: 10px; display: flex; justify-content: space-between; align-items: flex-start; font-size: 0.9rem; color: var(--text-muted); gap: 1rem;">
          <div style="flex: 1;">
             <span style="color:${c.color}; font-weight:bold;">➞</span> ${c.keywords}
          </div>
          <div style="flex: 1; text-align: right; font-style: italic; opacity: 0.8;">
            "${c.motto}"
          </div>
        </div>
        
        ${item.percent > 15 ? `
        <div style="margin-top: 6px;">
           <span class="tag-box" style="border: 1px solid ${c.color}44; color: #ccc;">
             ⚠️ Félelem: <span style="color: #fff;">${c.fear}</span>
           </span>
        </div>` : ''}
        
      </div>`;
  });
  
  // Trigger Animation
  setTimeout(() => {
    document.querySelectorAll('.bar-fill').forEach(f => f.style.width = f.dataset.width);
  }, 100);

  // --- 3. Chart.js Graph ---
  // Coordinates: X = Active vs Passive (D+I - S+C), Y = Task vs People (D+C - I+S)
  let xCoord = (scores.D + scores.I) - (scores.C + scores.S);
  let yCoord = (scores.D + scores.C) - (scores.I + scores.S);

  new Chart(document.getElementById('discChart'), {
    type: 'scatter',
    data: {
      datasets: [{
        label: 'Pozíciód',
        data: [{ x: xCoord, y: yCoord }],
        backgroundColor: dom.color,
        borderColor: '#fff',
        borderWidth: 3,
        pointRadius: 12,
        pointHoverRadius: 15
      }]
    },
    options: {
      responsive: true,
      maintainAspectRatio: true, // keeps it square
      layout: { padding: 10 },
      scales: {
        x: {
          min: -25, max: 25,
          grid: {
            color: (ctx) => ctx.tick.value === 0 ? 'rgba(255, 255, 255, 0.5)' : 'rgba(255, 255, 255, 0.05)',
            lineWidth: (ctx) => ctx.tick.value === 0 ? 2 : 1,
            drawBorder: false
          },
          title: { display: true, text: '⬅ Introvertált    Extrovertált ➡', color: '#94a3b8', font: { weight: 'bold' } },
          ticks: { display: false }
        },
        y: {
          min: -25, max: 25,
          grid: {
            color: (ctx) => ctx.tick.value === 0 ? 'rgba(255, 255, 255, 0.5)' : 'rgba(255, 255, 255, 0.05)',
            lineWidth: (ctx) => ctx.tick.value === 0 ? 2 : 1,
            drawBorder: false
          },
          title: { display: true, text: 'Kapcsolat ⬇    Feladat ⬆', color: '#94a3b8', font: { weight: 'bold' } },
          ticks: { display: false }
        }
      },
      plugins: {
        legend: { display: false },
        tooltip: {
          callbacks: {
            label: () => `D:${scores.D} I:${scores.I} S:${scores.S} C:${scores.C}`
          }
        }
      }
    }
  });
});
</script>
