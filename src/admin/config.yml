backend:
  name: github
  repo: Anderson-Hadnagy/Website # Path to your GitHub repo
  branch: main # The branch you want to edit
  base_url: https://your-oauth-proxy.com # We will create this in the next step

media_folder: "src/assets/uploads"
public_folder: "/assets/uploads"

collections:
  - name: "blog"
    # ... rest of your existing config
    label: "Blog Posts"
    folder: "src/posts" # Where the files will be saved
    create: true        # Allows you to create new posts
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Layout", name: "layout", widget: "hidden", default: "layouts/post.njk"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Date", name: "date", widget: "datetime"}
      - {label: "Featured Image", name: "image", widget: "image", required: false}
      - {label: "Body", name: "body", widget: "markdown"}
      - {label: "Tags", name: "tags", widget: "hidden", default: "post"}
